// Mock global objects, add global setup, etc.
jest.setTimeout(30000);  // Set timeout for all tests (optional)

// Optionally, mock specific modules or components
//jest.mock('axios'); // Mock the axios library if you're using it
